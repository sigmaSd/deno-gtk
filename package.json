{
  "name": "@sigmasd/gtk",
  "type": "module",
  "exports": {
    ".": "./src/gtk-ffi.ts"
  },
  "scripts": {
    "test": "deno fmt --check && deno lint && deno check --node-modules-dir=auto && deno test --allow-ffi"
  }
}
